import java.util.Scanner;

public class PracticeSet {
    public static void main(String[] args) {
        // Q1
        // Scanner in = new Scanner(System.in);

        // int Phy, Chem, Maths;

        // System.out.println("Enter marks in Physics: ");
        // Phy = in.nextInt();
        // System.out.println("Enter marks in Chemistry: ");
        // Chem = in.nextInt();
        // System.out.println("Enter marks in Maths: ");
        // Maths = in.nextInt();

        // in.close();

        // if((Phy + Chem + Maths)/3 >= 40){
        //     if(Phy >= 33 && Chem >= 33 && Maths >= 33){
        //         System.out.println("You have scored " + (Phy + Chem + Maths) + " and passed");
        //     }
        //     else{
        //         System.out.println("You have scored " + (Phy + Chem + Maths) + " but failed");
        //     }
        // }
        // else{
        //     System.out.println("You have scored " + (Phy + Chem + Maths) + " but failed");
        // }

        // Q4
        // Scanner in = new Scanner(System.in);

        // byte wDay = in.nextByte();
        // in.close();   --> Easy question
        int userScore = 0, sysScore = 0;
        System.out.println(userScore);
        System.out.println(sysScore);




    }
}
